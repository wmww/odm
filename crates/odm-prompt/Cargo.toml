[package]
name = "odm-prompt"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
doctest = false
# unit tests live in src/
test = true

[dependencies]
# Deliberately none: odm-cli must stay V8-free, and this is std-only anyway.

[dev-dependencies]
tempfile = "3"
